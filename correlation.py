#!/usr/bin/env python

'''
#TODO: write doc string
'''

import numpy as np

class Correlation:
    def __init__(self, name, raw_signal):
        '''
        name: what antennas is this the correlation of? Eg: (0, 0)
        raw_signal: An array of complex numbers
        '''
        self.name = name
        self.signal = signal

